s = input().strip()
groups = s.split("|")

decoded = []
for g in groups:
    if len(g) % 2 == 1:  
        decoded.append(g[1:] + g[0])
    else:                 
        decoded.append(g[-1] + g[:-1])

print("|".join(decoded))